https://www.codewars.com/kata/580a4001d6df740d61000301/solutions/python
def complete_series(seq): 
    a = [el for el in seq if seq.count(el)>1]
    if len(a)>0:
        return [0]
    else:
        return list(range(max(seq)+1))
